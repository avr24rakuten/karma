# Official ubuntu image
FROM avr24rakuten/karma_api:latest

ARG SECRET_JWT_KEY
ENV SECRET_JWT_KEY=$SECRET_JWT_KEY

ARG MYSQL_ROOT_PASSWORD
ENV MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD

EXPOSE 8000
WORKDIR /app
CMD uvicorn rakuten_api:server --host 0.0.0.0